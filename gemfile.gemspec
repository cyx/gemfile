# encoding: utf-8

Gem::Specification.new do |s|
  s.name              = "gemfile"
  s.version           = "0.0.1"
  s.summary           = "Library to generate a gemfile using installed gems."
  s.description       = "Library to generate a gemfile using installed gems."
  s.authors           = ["Cyril David", "Michel Martens"]
  s.email             = ["cyx@cyx.is", "michel@soveran.com"]
  s.homepage          = "http://github.com/cyx/gemfile"
  s.files             = ["bin/gemfile"]
  s.license           = "MIT"
  s.executables.push("gemfile")
end
